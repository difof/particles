{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Particles",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/Debug/particles",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb", // Use "gdb" if on Linux
            "preLaunchTask": "build-debug"
        }
    ]
}